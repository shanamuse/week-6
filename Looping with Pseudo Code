#include <stdio.h>
int main()
{
    int  x;
    
    printf("Enter a number X:\n");
    scanf("%d", &x);
    
    for( int n = 0; n < x; n++) {
    if ( n > 4 ) {
        printf("%d\n", n);
        
    }
    else 
    {
        printf("%d\n",9 - n);
    }
}
}
